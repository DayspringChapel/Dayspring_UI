services:
  - type: web
    name: dayspring-chapel
    env: node
    buildCommand: cd dayspring-chapel-nextjs && npm install && npm run build
    startCommand: cd dayspring-chapel-nextjs && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    rootDir: .